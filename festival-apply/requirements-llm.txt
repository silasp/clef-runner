# Optional: local Qwen LLM via Hugging Face transformers.
# Install with:  pip install -r requirements-llm.txt
# These are large; the app runs fine without them (deterministic fallback).
transformers>=4.40
torch>=2.2
accelerate>=0.30
sentencepiece>=0.2
